# Archived public screenshots

These legacy captures are retained as internal design references only. They were
removed from `frontend/public` because they include third-party overlays and must
not be shipped by the public production build. Do not move them back into a
public asset directory without a fresh content review.
